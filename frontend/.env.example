# Origin of the C# ray tracer render API. The Next dev/prod server proxies
# /api/* to this origin (see next.config.ts), so the browser never makes a
# cross-origin request. Copy to .env.local to override.
RAYTRACER_API_ORIGIN=http://localhost:5099
